<resources>

    <!-- Base application theme. -->
    <style name="DisasterMasterTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!-- Buttons -->
    <style name="btnBackTheme" parent="android:Widget.Button">
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:background">@color/colorAccent</item>
    </style>

    <style name="btnGoTheme" parent="android:Widget.Button">
        <item name="android:textColor">@color/colorPrimaryCards</item>
        <item name="android:background">@color/colorYellow</item>
    </style>


</resources>
